<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitTracker">
        <activity
            android:name=".activities.ProfileActivity"
            android:exported="false" />
        <!-- Dashboard -->
        <activity
            android:name=".activities.DashboardActivity"
            android:exported="false" /> <!-- Recuperar Palavra-Passe -->
        <activity
            android:name=".activities.RecoverPasswordActivity"
            android:exported="false" /> <!-- Registar -->
        <activity
            android:name=".activities.SignUpActivity"
            android:exported="false" /> <!-- Iniciar Treino -->
        <activity
            android:name=".activities.StartTrainingActivity"
            android:exported="false" /> <!-- Login (Activity Principal) -->
        <activity
            android:name=".activities.LogInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>